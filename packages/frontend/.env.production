# Read by `vite build` only — a dev server sends no analytics.
#
# The PostHog PROJECT key is public by design: it can only write events, and
# it ships in the page source of every site that uses PostHog. It is committed
# so the deploy script needs no extra configuration on the box. The private
# (personal) API key is a different thing and must never appear here.
VITE_POSTHOG_KEY=phc_vhx584HthxJgHJBLFAjW8AhS2vRpczDtaC5bvzFnVaf7
VITE_POSTHOG_HOST=https://eu.i.posthog.com
